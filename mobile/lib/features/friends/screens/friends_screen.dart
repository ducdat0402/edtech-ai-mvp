import 'package:flutter/material.dart';
import 'package:edtech_mobile/features/community/screens/community_hub_screen.dart';

/// Route `/friends` — hub Cộng đồng + Bạn bè.
class FriendsScreen extends StatelessWidget {
  const FriendsScreen({super.key});

  @override
  Widget build(BuildContext context) => const CommunityHubScreen();
}
